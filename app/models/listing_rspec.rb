require 'rails helper'

# Rspec.describe.listing, :'type' :'model' do
# 	context 'validates title' do 
		# it { is expected to validate_presence_of(:title)}
		# # it { is expected to validate_uniqueness_of(:title)}
		# it { is expected to ensure_length_of(:title) is at least(25) is atmost(1000)}
	# end
end